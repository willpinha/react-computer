import { Container, TextInput } from "@mantine/core";

export function Index() {
	return (
		<Container size="xs">
			<TextInput
				placeholder="Type something here..."
				variant="unstyled"
			/>
		</Container>
	);
}
